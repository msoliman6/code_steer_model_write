# The runtime dependencies -- the one owner (rule 4). doctor reads this file and halts naming
# the install line when one is missing:
#   python3 -m pip install -r requirements.txt
pydantic>=2.5
pydantic-settings>=2.2
jinja2>=3.1
prefect>=3.0
mlflow>=2.16
reflex>=0.6
anthropic>=0.40
litellm>=1.50
cedarpy>=4.8
guardrails-ai>=0.11
pydantic-ai-slim[anthropic,openai]>=2.39
mcp>=2.1
typer>=0.19,<0.28
